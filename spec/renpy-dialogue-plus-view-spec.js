'use babel';

import RenpyDialoguePlusView from '../lib/renpy-dialogue-plus-view';

describe('RenpyDialoguePlusView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
